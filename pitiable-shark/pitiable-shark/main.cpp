#include <iostream>
#include <string>

#include "Game.h"
#include "Story.h"

int main() {
    /*Output test;
    test.setColors(0);
    test.talk("今天是個美好的一天");
    test.PauseSomeTimes(1);
    test.setColors(1);
    test.talk("今天是個美好的一天");
    test.PauseSomeTimes(1);
    test.setColors(2);
    test.talk("今天是個美好的一天");
    Story story;
    story.outPutFile("test.txt");
    system("pause");*/
    //暫時用來測試。
    

    Game game;
    game.runTheGame();
    return 0;
}
